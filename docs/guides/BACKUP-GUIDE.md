# دليل النسخ الاحتياطي والاستعادة من الكوارث
# Backup and Disaster Recovery Guide

## 🔴 حرج - يجب القراءة قبل الإطلاق

هذا الدليل يوضح كيفية إعداد نظام نسخ احتياطي آمن لقاعدة البيانات والملفات الحرجة.

---

## 📋 ملخص سريع

**ما يجب نسخه احتياطيًا:**
- ✅ قاعدة البيانات (MySQL/Supabase) - **يوميًا**
- ✅ المتغيرات البيئية (.env) - **عند كل تغيير**
- ✅ الملفات المرفوعة (إن وجدت) - **يوميًا**
- ✅ السجلات (logs/) - **أسبوعيًا**

**التردد الموصى به:**
- نسخ احتياطي يومي تلقائي
- الاحتفاظ بـ 30 يومًا على الأقل
- اختبار الاستعادة شهريًا

---

## 1️⃣ النسخ الاحتياطي لقاعدة البيانات

### الطريقة الأولى: Supabase Dashboard (موصى بها)

إذا كنت تستخدم Supabase:

1. افتح [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروعك
3. اذهب إلى **Settings** → **Database**
4. قسم **Backups**:
   - تفعيل **Point-in-time Recovery (PITR)** للنسخ المستمر
   - تفعيل **Daily Backups** للنسخ اليومية
5. لتحميل نسخة يدوية:
   ```bash
   # من لوحة التحكم: Database → Backups → Download
   ```

### الطريقة الثانية: mysqldump (للخوادم الخاصة)

```bash
#!/bin/bash
# backup-database.sh

# إعدادات
DB_HOST="your-database-host"
DB_NAME="stylora"
DB_USER="your-username"
DB_PASS="your-password"
BACKUP_DIR="/path/to/backups"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="$BACKUP_DIR/stylora_$DATE.sql.gz"

# إنشاء مجلد النسخ الاحتياطية
mkdir -p $BACKUP_DIR

# النسخ الاحتياطي
mysqldump -h $DB_HOST -u $DB_USER -p$DB_PASS $DB_NAME | gzip > $BACKUP_FILE

# التحقق من النجاح
if [ $? -eq 0 ]; then
    echo "✅ Backup successful: $BACKUP_FILE"
    
    # حذف النسخ الأقدم من 30 يوم
    find $BACKUP_DIR -name "stylora_*.sql.gz" -mtime +30 -delete
else
    echo "❌ Backup failed!"
    exit 1
fi
```

**جدولة النسخ الاحتياطي اليومي:**

```bash
# تعديل crontab
crontab -e

# إضافة سطر للنسخ الاحتياطي اليومي في الساعة 3 صباحًا
0 3 * * * /path/to/backup-database.sh >> /var/log/backup.log 2>&1
```

---

## 2️⃣ النسخ الاحتياطي للمتغيرات البيئية

**⚠️ تحذير:** ملف `.env` يحتوي على معلومات حساسة جدًا!

### الطريقة الآمنة:

```bash
# نسخ .env إلى مكان آمن ومشفر
cp .env .env.backup

# تشفير النسخة الاحتياطية
gpg --symmetric --cipher-algo AES256 .env.backup

# حذف النسخة غير المشفرة
rm .env.backup

# الآن لديك .env.backup.gpg مشفر
# احفظه في مكان آمن (خارج السيرفر!)
```

### الاستعادة:

```bash
# فك التشفير
gpg --decrypt .env.backup.gpg > .env

# التحقق من المحتوى
cat .env
```

**أماكن آمنة لحفظ النسخ الاحتياطية:**
- ✅ مدير كلمات السر (1Password, Bitwarden)
- ✅ خدمة تخزين مشفرة (Dropbox مع تشفير)
- ✅ قبو آمن (Safe deposit box) للنسخ الحرجة
- ❌ **لا تحفظها في:**
  - Git repository
  - بريد إلكتروني
  - ملاحظات غير مشفرة

---

## 3️⃣ النسخ الاحتياطي للملفات المرفوعة

إذا كان المشروع يخزن ملفات مرفوعة (صور، مستندات):

```bash
#!/bin/bash
# backup-uploads.sh

UPLOADS_DIR="/path/to/uploads"
BACKUP_DIR="/path/to/backups/uploads"
DATE=$(date +%Y%m%d)

# نسخ الملفات
rsync -av --delete $UPLOADS_DIR/ $BACKUP_DIR/$DATE/

# ضغط النسخة
tar -czf $BACKUP_DIR/uploads_$DATE.tar.gz -C $BACKUP_DIR $DATE

# حذف المجلد غير المضغوط
rm -rf $BACKUP_DIR/$DATE

echo "✅ Uploads backed up: uploads_$DATE.tar.gz"
```

---

## 4️⃣ النسخ الاحتياطي للسجلات

```bash
#!/bin/bash
# backup-logs.sh

LOGS_DIR="/path/to/logs"
BACKUP_DIR="/path/to/backups/logs"
DATE=$(date +%Y%m%d)

# ضغط السجلات
tar -czf $BACKUP_DIR/logs_$DATE.tar.gz -C $LOGS_DIR .

# حذف السجلات الأقدم من 90 يوم
find $BACKUP_DIR -name "logs_*.tar.gz" -mtime +90 -delete

echo "✅ Logs backed up: logs_$DATE.tar.gz"
```

---

## 5️⃣ استعادة قاعدة البيانات

### من Supabase:

1. اذهب إلى **Database** → **Backups**
2. اختر النسخة المطلوبة
3. اضغط **Restore**
4. تأكيد الاستعادة

### من ملف mysqldump:

```bash
# فك الضغط والاستعادة
gunzip < stylora_20240101_030000.sql.gz | mysql -h $DB_HOST -u $DB_USER -p$DB_PASS $DB_NAME

# أو بخطوتين
gunzip stylora_20240101_030000.sql.gz
mysql -h $DB_HOST -u $DB_USER -p$DB_PASS $DB_NAME < stylora_20240101_030000.sql
```

**⚠️ تحذير:** الاستعادة تستبدل جميع البيانات الحالية!

---

## 6️⃣ اختبار النسخ الاحتياطي

**يجب اختبار الاستعادة شهريًا!**

### خطوات الاختبار:

1. **إنشاء بيئة اختبار:**
   ```bash
   # إنشاء قاعدة بيانات اختبار
   mysql -u root -p -e "CREATE DATABASE stylora_test;"
   ```

2. **استعادة النسخة الاحتياطية:**
   ```bash
   gunzip < latest_backup.sql.gz | mysql -u root -p stylora_test
   ```

3. **التحقق من البيانات:**
   ```bash
   mysql -u root -p stylora_test -e "SELECT COUNT(*) FROM customers;"
   mysql -u root -p stylora_test -e "SELECT COUNT(*) FROM appointments;"
   ```

4. **حذف قاعدة الاختبار:**
   ```bash
   mysql -u root -p -e "DROP DATABASE stylora_test;"
   ```

---

## 7️⃣ سيناريوهات الكوارث وحلولها

### السيناريو 1: فقدان قاعدة البيانات الكاملة

**الحل:**
1. استعادة آخر نسخة احتياطية
2. تطبيق transaction logs (إن وجدت)
3. التحقق من سلامة البيانات
4. إعادة تشغيل التطبيق

**الوقت المتوقع:** 15-30 دقيقة

---

### السيناريو 2: حذف بيانات مستأجر بالخطأ

**الحل:**
1. إيقاف التطبيق فورًا
2. استعادة النسخة الاحتياطية في قاعدة مؤقتة
3. استخراج بيانات المستأجر المحذوف
4. إدراج البيانات في القاعدة الرئيسية
5. التحقق من البيانات
6. إعادة تشغيل التطبيق

**SQL للاستخراج:**
```sql
-- استخراج بيانات مستأجر محدد
SELECT * FROM customers WHERE tenantId = 'tenant-id' INTO OUTFILE '/tmp/customers.csv';
SELECT * FROM appointments WHERE tenantId = 'tenant-id' INTO OUTFILE '/tmp/appointments.csv';
-- ... إلخ
```

---

### السيناريو 3: اختراق أمني

**الحل:**
1. **إيقاف التطبيق فورًا**
2. تغيير جميع كلمات السر والمفاتيح
3. فحص السجلات لتحديد نطاق الاختراق
4. استعادة من نسخة احتياطية قبل الاختراق
5. تصحيح الثغرة الأمنية
6. إبلاغ المستخدمين المتأثرين
7. مراجعة أمنية شاملة

---

## 8️⃣ قائمة التحقق قبل الإطلاق

- [ ] تفعيل النسخ الاحتياطي اليومي التلقائي
- [ ] اختبار استعادة نسخة احتياطية بنجاح
- [ ] حفظ نسخة من .env في مكان آمن ومشفر
- [ ] إعداد تنبيهات لفشل النسخ الاحتياطي
- [ ] توثيق إجراءات الاستعادة للفريق
- [ ] تحديد شخص مسؤول عن النسخ الاحتياطي
- [ ] جدولة اختبارات استعادة شهرية
- [ ] التأكد من تخزين النسخ في موقع منفصل عن السيرفر

---

## 9️⃣ أدوات موصى بها

### خدمات النسخ الاحتياطي المدارة:

1. **Supabase Backups** (مدمج)
   - نسخ احتياطي تلقائي
   - Point-in-time recovery
   - سهل الاستخدام

2. **AWS RDS Automated Backups**
   - نسخ احتياطي يومي
   - الاحتفاظ حتى 35 يوم
   - استعادة سريعة

3. **DigitalOcean Managed Databases**
   - نسخ احتياطي يومي
   - استعادة بضغطة زر
   - تكلفة معقولة

### أدوات مراقبة النسخ الاحتياطي:

- **Healthchecks.io** - تنبيهات عند فشل النسخ الاحتياطي
- **Cronitor** - مراقبة cron jobs
- **UptimeRobot** - مراقبة توفر التطبيق

---

## 🆘 جهات الاتصال في حالات الطوارئ

**مسؤول النظام:**
- الاسم: _______________
- الهاتف: _______________
- البريد: _______________

**مسؤول قاعدة البيانات:**
- الاسم: _______________
- الهاتف: _______________
- البريد: _______________

**الدعم الفني (Supabase/AWS):**
- الرابط: _______________
- رقم الحساب: _______________

---

## 📚 موارد إضافية

- [Supabase Backup Documentation](https://supabase.com/docs/guides/platform/backups)
- [MySQL Backup Best Practices](https://dev.mysql.com/doc/refman/8.0/en/backup-and-recovery.html)
- [3-2-1 Backup Rule](https://www.backblaze.com/blog/the-3-2-1-backup-strategy/)

---

**آخر تحديث:** ديسمبر 2024  
**المراجعة التالية:** يناير 2025
