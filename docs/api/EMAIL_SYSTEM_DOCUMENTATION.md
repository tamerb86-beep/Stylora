# نظام البريد الإلكتروني في Stylora SaaS

## الملخص التنفيذي

نظام البريد الإلكتروني في Stylora **مُطبق بالكامل وجاهز للاستخدام**. يستخدم النظام **Nodemailer** مع SMTP لإرسال رسائل بريد إلكترونية احترافية باللغة النرويجية للعملاء.

---

## الميزات المُطبقة ✅

### 1. إرسال البريد الإلكتروني التلقائي

النظام يرسل رسائل بريد إلكترونية تلقائياً في الحالات التالية:

#### أ) تأكيد الموعد (Booking Confirmation)
- **المُشغل:** عند تغيير حالة الموعد إلى "confirmed"
- **المحتوى:**
  - اسم الصالون
  - اسم العميل
  - التاريخ والوقت
  - قائمة الخدمات
- **التصميم:** قالب HTML احترافي بألوان أرجوانية
- **الموقع في الكود:** `server/email.ts` → `renderBookingConfirmationEmail()`

#### ب) إلغاء الموعد (Booking Cancellation)
- **المُشغل:** عند تغيير حالة الموعد إلى "canceled"
- **المحتوى:**
  - اسم الصالون
  - اسم العميل
  - التاريخ والوقت المُلغى
- **التصميم:** قالب HTML بألوان برتقالية/حمراء
- **الموقع في الكود:** `server/email.ts` → `renderBookingCancellationEmail()`

#### ج) إرسال الإيصال (Receipt Email)
- **المُشغل:** عند طلب العميل إرسال الإيصال عبر البريد الإلكتروني
- **المحتوى:**
  - رسالة شكر
  - رقم الطلب
  - المبلغ الإجمالي
  - ملف PDF مرفق للإيصال
- **التصميم:** قالب HTML بألوان زرقاء/برتقالية
- **الموقع في الكود:** `server/email.ts` → `sendReceiptEmail()`

---

## البنية التقنية

### الملفات الرئيسية

```
server/
├── email.ts                          # نظام إرسال البريد الإلكتروني الأساسي
├── notifications-appointments.ts     # إرسال تلقائي للمواعيد
├── emailService.ts                   # خدمة التحقق من البريد الإلكتروني
└── _core/env.ts                      # إعدادات SMTP
```

### المتغيرات البيئية المطلوبة

```bash
# إعدادات SMTP
SMTP_HOST=smtp.gmail.com              # خادم SMTP (مثال: Gmail)
SMTP_PORT=587                         # منفذ SMTP (587 للتشفير TLS، 465 لـ SSL)
SMTP_USER=your-email@gmail.com        # اسم المستخدم SMTP
SMTP_PASS=your-app-password           # كلمة مرور SMTP (أو App Password)
SMTP_FROM_EMAIL=no-reply@stylora.app  # عنوان البريد المُرسل
```

### الدوال الرئيسية

#### 1. `sendEmail(options)`
الدالة الأساسية لإرسال البريد الإلكتروني.

```typescript
import { sendEmail } from "./email";

await sendEmail({
  to: "customer@example.com",
  subject: "موضوع الرسالة",
  html: "<h1>محتوى HTML</h1>"
});
```

**الميزات:**
- معالجة الأخطاء التلقائية (لا تُعطل العملية الأساسية)
- تخطي الإرسال إذا لم يكن SMTP مُكوّناً
- سجلات تفصيلية في الكونسول

#### 2. `sendAppointmentConfirmationIfPossible(appointmentId, tenantId)`
إرسال تأكيد الموعد تلقائياً.

```typescript
import { sendAppointmentConfirmationIfPossible } from "./notifications-appointments";

await sendAppointmentConfirmationIfPossible(123, "tenant-id");
```

**الشروط:**
- العميل يجب أن يكون لديه عنوان بريد إلكتروني
- الموعد يجب أن يكون موجوداً في قاعدة البيانات
- SMTP يجب أن يكون مُكوّناً

#### 3. `sendAppointmentCancellationIfPossible(appointmentId, tenantId)`
إرسال إشعار إلغاء الموعد تلقائياً.

```typescript
import { sendAppointmentCancellationIfPossible } from "./notifications-appointments";

await sendAppointmentCancellationIfPossible(123, "tenant-id");
```

---

## نقاط التشغيل التلقائي

### 1. عند تحديث حالة الموعد (Manual Status Update)

في `server/routers.ts` → `appointments.updateStatus`:

```typescript
// عند التأكيد
if (previousStatus !== "confirmed" && input.status === "confirmed") {
  sendAppointmentConfirmationIfPossible(input.id, String(ctx.tenantId));
}

// عند الإلغاء
if (previousStatus !== "canceled" && input.status === "canceled") {
  sendAppointmentCancellationIfPossible(input.id, String(ctx.tenantId));
}
```

### 2. عند الدفع عبر Stripe (Webhook)

في `server/stripe-webhook.ts`:

```typescript
// بعد تأكيد الدفع
await sendAppointmentConfirmationIfPossible(appointmentId, tenantId);
```

### 3. عند الدفع عبر Vipps (Callback)

في `server/vipps-callback.ts`:

```typescript
// بعد تأكيد الدفع
await sendAppointmentConfirmationIfPossible(appointment.id, payment.tenantId);
```

### 4. عند طلب إرسال الإيصال

في `server/routers.ts` → `pos.sendReceiptEmail`:

```typescript
await sendReceiptEmail({
  to: input.customerEmail,
  salonName: order.tenant.name,
  customerName: customerName,
  orderId: order.order.id,
  total: `${order.order.totalAmount.toFixed(2)} NOK`,
  pdfBuffer: pdfBuffer,
  filename: filename,
});
```

---

## قوالب البريد الإلكتروني (Email Templates)

### تصميم موحد

جميع القوالب تستخدم:
- **خطوط:** -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto
- **عرض أقصى:** 600px (متوافق مع الأجهزة المحمولة)
- **ألوان متدرجة** في الرأس
- **محتوى HTML نظيف** مع CSS مضمّن

### 1. قالب تأكيد الموعد

```
┌─────────────────────────────────┐
│  ✂️ Timebestilling bekreftet   │  ← رأس أرجواني
├─────────────────────────────────┤
│ Hei [اسم العميل],              │
│                                 │
│ ┌─────────────────────────────┐ │
│ │ 📅 Dato: [التاريخ]          │ │
│ │ 🕐 Klokkeslett: [الوقت]     │ │
│ │ 💇 Tjenester: [الخدمات]     │ │
│ └─────────────────────────────┘ │
│                                 │
│ Velkommen!                      │
│ Hilsen [اسم الصالون]           │
└─────────────────────────────────┘
```

### 2. قالب إلغاء الموعد

```
┌─────────────────────────────────┐
│  ❌ Timebestilling kansellert  │  ← رأس برتقالي/أحمر
├─────────────────────────────────┤
│ Hei [اسم العميل],              │
│                                 │
│ ┌─────────────────────────────┐ │
│ │ 📅 Dato: [التاريخ]          │ │
│ │ 🕐 Klokkeslett: [الوقت]     │ │
│ └─────────────────────────────┘ │
│                                 │
│ Ta gjerne kontakt...            │
│ Hilsen [اسم الصالون]           │
└─────────────────────────────────┘
```

### 3. قالب إرسال الإيصال

```
┌─────────────────────────────────┐
│  🧾 Kvittering                  │  ← رأس أزرق/برتقالي
├─────────────────────────────────┤
│ Hei [اسم العميل],              │
│ Takk for besøket!               │
│                                 │
│ ┌─────────────────────────────┐ │
│ │ 📋 Ordre-ID: #[رقم]         │ │
│ │ 💰 Total: [المبلغ]          │ │
│ └─────────────────────────────┘ │
│                                 │
│ 📎 Vedlegg: PDF kvittering      │
│                                 │
│ Velkommen tilbake!              │
│ Hilsen [اسم الصالون]           │
└─────────────────────────────────┘
```

---

## الاختبارات (Tests)

النظام يحتوي على مجموعة شاملة من الاختبارات:

### ملف الاختبار الرئيسي
`server/email-notifications.test.ts`

### الاختبارات المُطبقة:

1. ✅ **عرض قالب تأكيد الموعد** - يتحقق من صحة HTML والنص النرويجي
2. ✅ **عرض قالب إلغاء الموعد** - يتحقق من صحة HTML والنص النرويجي
3. ✅ **إرسال تأكيد عند تغيير الحالة** - يختبر التشغيل التلقائي
4. ✅ **عدم إرسال تأكيد مكرر** - يمنع الإرسال المتكرر
5. ✅ **إرسال إلغاء عند الإلغاء** - يختبر إشعار الإلغاء
6. ✅ **عدم إرسال إلغاء مكرر** - يمنع الإرسال المتكرر
7. ✅ **تخطي الإرسال للعملاء بدون بريد إلكتروني** - معالجة الحالات الخاصة

---

## الحالة الحالية

### ✅ ما يعمل:
1. **النظام الأساسي** - Nodemailer مُكوّن بالكامل
2. **القوالب** - 3 قوالب HTML احترافية بالنرويجية
3. **التشغيل التلقائي** - يعمل عند تحديث حالة الموعد
4. **التكامل مع Stripe** - يرسل تأكيد بعد الدفع
5. **التكامل مع Vipps** - يرسل تأكيد بعد الدفع
6. **إرسال الإيصالات** - مع مرفقات PDF
7. **معالجة الأخطاء** - لا تُعطل العملية الأساسية
8. **الاختبارات** - مجموعة شاملة من الاختبارات

### ⚠️ ما يحتاج إلى إعداد:

1. **إعدادات SMTP** - يجب تكوين المتغيرات البيئية:
   ```bash
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASS=your-app-password
   SMTP_FROM_EMAIL=no-reply@stylora.app
   ```

2. **واجهة المستخدم لإعدادات البريد الإلكتروني** - لا توجد صفحة إدارية لإدارة إعدادات SMTP (حالياً يتم التكوين عبر المتغيرات البيئية فقط)

---

## كيفية تفعيل النظام

### الخطوة 1: تكوين SMTP

#### خيار أ: استخدام Gmail

1. إنشاء حساب Gmail (أو استخدام حساب موجود)
2. تفعيل "2-Step Verification"
3. إنشاء "App Password":
   - الذهاب إلى: https://myaccount.google.com/apppasswords
   - اختيار "Mail" و "Other (Custom name)"
   - نسخ كلمة المرور المُنشأة (16 حرف)
4. تعيين المتغيرات البيئية:
   ```bash
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-gmail@gmail.com
   SMTP_PASS=xxxx-xxxx-xxxx-xxxx  # App Password
   SMTP_FROM_EMAIL=no-reply@stylora.app
   ```

#### خيار ب: استخدام خدمة SMTP مخصصة

خدمات موصى بها:
- **SendGrid** - 100 بريد/يوم مجاناً
- **Mailgun** - 5,000 بريد/شهر مجاناً (أول 3 أشهر)
- **Amazon SES** - رخيص جداً ($0.10 لكل 1000 بريد)
- **Postmark** - احترافي للمعاملات

### الخطوة 2: إعادة تشغيل الخادم

بعد تعيين المتغيرات البيئية:
```bash
cd /home/ubuntu/stylora
pnpm db:push  # إذا لزم الأمر
# سيتم إعادة تشغيل الخادم تلقائياً
```

### الخطوة 3: الاختبار

1. إنشاء موعد جديد
2. تغيير حالة الموعد إلى "confirmed"
3. التحقق من سجلات الخادم:
   ```
   [Email] Sent successfully to: customer@example.com
   [Email] Confirmation email sent for appointment: 123
   ```
4. التحقق من صندوق البريد الوارد للعميل

---

## الأسئلة الشائعة

### س: لماذا لا يتم إرسال البريد الإلكتروني؟

**ج:** تحقق من:
1. ✅ هل SMTP مُكوّن؟ (تحقق من المتغيرات البيئية)
2. ✅ هل العميل لديه عنوان بريد إلكتروني في قاعدة البيانات؟
3. ✅ هل تظهر رسائل خطأ في سجلات الخادم؟
4. ✅ هل كلمة مرور SMTP صحيحة؟

### س: هل يمكن تخصيص القوالب؟

**ج:** نعم! عدّل الدوال في `server/email.ts`:
- `renderBookingConfirmationEmail()` - قالب التأكيد
- `renderBookingCancellationEmail()` - قالب الإلغاء
- `renderReceiptEmail()` - قالب الإيصال

### س: هل يدعم النظام مرفقات أخرى غير PDF؟

**ج:** نعم! استخدم نفس طريقة `sendReceiptEmail()` وأضف المرفقات في `attachments` array.

### س: كيف يمكن إضافة لغات أخرى؟

**ج:** أضف معامل `language` للقوالب واستخدم شرط `if/else` لاختيار النص المناسب.

### س: هل يمكن إرسال بريد إلكتروني يدوياً؟

**ج:** نعم! استخدم الدالة `sendEmail()` مباشرة:
```typescript
import { sendEmail } from "./server/email";

await sendEmail({
  to: "customer@example.com",
  subject: "رسالة مخصصة",
  html: "<p>محتوى الرسالة</p>"
});
```

---

## التوصيات

### أولوية عالية:
1. ✅ **تكوين SMTP** - ضروري لتفعيل النظام
2. 📝 **إنشاء واجهة إدارية** - لإدارة إعدادات SMTP من لوحة التحكم
3. 🧪 **اختبار شامل** - اختبار الإرسال الفعلي مع SMTP حقيقي

### أولوية متوسطة:
4. 🌍 **دعم لغات متعددة** - إضافة الإنجليزية والعربية
5. 📊 **سجل البريد الإلكتروني** - حفظ سجل لجميع الرسائل المُرسلة
6. 🔔 **إشعارات إضافية** - تذكيرات قبل 24 ساعة، رسائل ترحيب، إلخ

### أولوية منخفضة:
7. 🎨 **قوالب قابلة للتخصيص** - السماح للصالونات بتخصيص القوالب
8. 📈 **تحليلات البريد الإلكتروني** - معدل الفتح، النقرات، إلخ
9. 🔄 **إعادة المحاولة التلقائية** - إعادة إرسال الرسائل الفاشلة

---

## الخلاصة

نظام البريد الإلكتروني في Stylora **مُطبق بالكامل ومُختبر جيداً**. كل ما يحتاجه هو **تكوين إعدادات SMTP** ليصبح جاهزاً للعمل. النظام يرسل رسائل احترافية بالنرويجية تلقائياً عند تأكيد أو إلغاء المواعيد، ويدعم إرسال الإيصالات مع مرفقات PDF.

**الحالة:** 🟢 جاهز للإنتاج (بعد تكوين SMTP)
